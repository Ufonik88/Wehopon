# Pinned dev dependencies for faster `pip install` resolution.
# If `pip install -e ".[dev]"` times out (issue O6), install these explicitly
# and then `pip install -e .` to get the runtime deps.

pytest>=8.0.0,<10
pytest-cov>=5.0.0,<8
ruff>=0.8.0,<1
mypy>=1.10.0,<3
httpx>=0.27.0,<1
pre-commit>=3.7.0,<5
